version: '3'

services:
  database:
    image: mysql:8.0
    environment:
      - MYSQL_DATABASE=hilde
      - MYSQL_ROOT_PASSWORD=hilde
    ports:
      - "3309:3306"
    volumes:
      - db:/var/lib/mysql

volumes:
  db:
